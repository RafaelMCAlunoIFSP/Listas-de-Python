A = float(input("Digite o primeiro valor: "))
B = float(input("Digite o segundo valor: "))

if A > B:
    
    diferenca = A - B

    print(f"Diferença entre {A} e {B}: {diferenca}")

else:

    diferenca = B - A

    print(f"Diferença entre {B} e {A}: {diferenca}")